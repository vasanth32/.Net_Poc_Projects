version: '3.8'
services:
  consul:
    image: consul:1.15.4
    container_name: consul-dev
    ports:
      - "8500:8500" # Consul UI
      - "8600:8600/udp" # DNS (optional)
    command: agent -dev -client=0.0.0.0
    # -dev: single-node dev mode
    # -client=0.0.0.0: allow connections from host 